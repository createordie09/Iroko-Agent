# Feuille de Route d'Intégration Iroko Code Agent — Lots L1 à L15 (`docs/ROADMAP.md`)

Ce document structure l'état d'avancement réel et l'ordonnancement séquentiel des 15 lots prioritaires (**L1 à L15**). Il intègre les dépendances techniques, l'état constaté dans le code, les risques pour l'interface figée et les critères d'acceptation stricts issus de la section §34 du cahier des charges.

---

## Synthèse d'Avancement des Lots L1 à L15

| Lot | Intitulé & Périmètre Technique | Sections Cahier | Dépendances | État Réel constaté | Risque UI |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **L1** | Runtime Sûr (127.0.0.1, token bootstrap mémoire, persistance runtime hors workspace, Origin/Host guard), Observabilité & Sidebar Discussions | **§21, §24, §26, §29, §36** | Aucune | **Fait (100%)** | Bas |
| **L2** | Fournisseurs & Clés (AES-256-GCM IV unique aléatoire, trousseau hors dépôt), Model Gateway, Rotation 429 & Sélecteur de modèle *(Page Fournisseurs & Clés)* | **§10, §26** | L1 | **Fait (100%)** | Bas |
| **L3** | Chat Réel (Streaming, Bloc Réflexion conditionnel, Arrêt AbortController, Titre auto, Markdown sécurisé) *(Page Réfléchir)* | **§22, §37** | L1, L2 | **Fait (100%)** | Bas |
| **L4** | Préférences (Police, Animations, Voix, Notifications) *(Page Préférences)* | **hors cahier** | L1 | **Fait (100%)** | Bas |
| **L5** | Moteur de Permissions (4 portées, magasin hors workspace, risque commandes composées) *(Page Iroko Code)* | **§9, §26** | L1 | **Fait (100%)** | Moyen |
| **L6** | Workspace Manager, Règles projet (AGENTS.md, IROKO.md), Chemins Windows exhaustifs (caseless, UNC, realpath), Outils Fichiers & Diffs | **§7, §12, §19, §26, §31** | L1, L5 | **Fait (100%)** | Bas |
| **L7** | Boucle Agentique avec Limites (§37), Définition du progrès (sans modif de fichier) & Délais sans attente permission | **§5, §37** | L1 à L6 | **Fait (100%)** | Bas |
| **L8** | Terminal, Environnement Assaini (zéro secret hérité) & Processus Multi-Plateformes (Arbre garanti taskkill /T /F) | **§8, §26** | L1, L5, L7 | **Fait (100%)** | Bas |
| **L9** | Git (Outils natifs, affichage branche dans en-tête Modifications) | **§17** | L1, L6 | **Fait (100%)** | Bas |
| **L10** | Vérification Automatisée (Détection dynamique lockfiles npm/pnpm/yarn/bun, scripts réels, tsc, eslint, tests, build) | **§6.6, §6.7, §18** | L1, L5, L8 | **Fait (100%)** | Bas |
| **L11** | Boucle Autonome Évoluée & Gestionnaire de Plan Dynamique Auto-Replanifiant | **§5.2, §20.1, §21, §22** | L1 à L10 | **Fait (100%)** | Bas |
| **L12** | Mémoire de Projet (§20) vs Persistance de Session (§21) (Zéro secret mémorisé) *(Page Mémoire)* | **§20, §21** | L1, L11 | **Fait (100%)** | Bas |
| **L13** | Capacités (dynamique Tool Registry) + Confidentialité & Caviardage automatique *(Pages Capacités & Confidentialité)* | **§6, §26** | L1, L2, L5 | **Fait (100%)** | Bas |
| **L14** | Model Context Protocol (MCP) sécurisé (sanitization outils, interdiction chargement auto projet) + Compétences + Connecteurs | **§13, §15, §19, §26** | L1, L3, L5 | **Fait (100%)** | Moyen |
| **L15** | Capacités Avancées (15a LSP 100%, 15b Navigateur Playwright 100%, 15c Sous-Agents 100%, 15d Plugins 100%, 15e Thème Clair 100%) | **§11, §14, §16** | L1 à L14 | **Fait (100%)** | Bas |
| **M1** | **Tout se fait dans le Chat** : Unification agent de code dans l'interface de conversation unique, retrait bouton Code sidebar, sélecteur [Chat \| Code] composer, inspecteur enrichi (diffs, plan, terminal, tests), permissions in-chat | **§5 à §23** | L1 à L15 | **Fait (100%)** | Bas |
| **M2** | **Joindre tout fichier qu'une IA peut lire** : Menu '+', glisser-déposer, collage images, streaming disque hors workspace, magic bytes, multi-encodage, formats Office/PDF/texte/archives, pagination `read_attachment`, chips composer et aperçu inspecteur | **§26** | M1 | **Fait (100%)** | Bas |
| **M3** | **Travailler dans un dossier de mon PC** : Dialogue natif OS (STA/TopMost Windows, osascript macOS, zenity Linux), validation serveur stricte (rejet racines, répertoires système et runtime, warning home), menu '+' avec récents et fermeture, puce projet composer, verrou mono-rédacteur avec mode lecture seule, espaces temporaires isolés 100 Mo avec confinement anti-traversée et routine de copie PC | **§12, §26** | M1, M2 | **Fait (100%)** | Bas |
| **M4** | **Copier en un clic et générer des artéfacts** : Blocs de code CommonMark avec copie fiable sans saut visuel et coloration sobre ; artéfacts texte hors workspace avec cartes in-chat, versions v1..vN, restauration, téléchargement sécurisé, onglet inspecteur dédié et directives prompt système v1.1.0 | **§11, §26, §37** | M1 à M3 | **Fait (100%)** | Bas |
| **M5** | **Tous les formats de fichiers, sans risque** : Niveaux de génération N2 bureautique (`docx`, `xlsx`, `pptx`, `pdf`, `zip`) via `create_document` validé par Zod & N3 fichiers créés par scripts via `register_artifact` (confinement strict, taille ≤ 50 Mo) ; aperçu HTML dans iframe sandboxée sans réseau (`allow-scripts`, sans `allow-same-origin`, meta CSP `connect-src 'none'`) ; aperçu SVG via balise `<img>` isolée ; aperçus bureautiques docx/xlsx/pptx/pdf ; téléchargement groupé en archive ZIP (`/api/conversations/:id/artifacts/download-all`) | **§26** | M1 à M4 | **Fait (100%)** | Bas |
| **M6** | **Génération et manipulation d'images** : Passerelle Média dédiée séparée du chat (`MediaProvider`, `MediaGateway`), adaptateurs OpenAI (`b64_json`/URL avec garde SSRF), Google Imagen 3 (`predict`), Cloudflare Workers AI avec Flux-1-Schnell (décodage base64 du JSON) et Mock ; configuration dans Paramètres › Fournisseurs & Clés (chiffrement AES-256-GCM) ; outil `generate_image` accessible en Chat et Code ; ligne "Créer une image" dans le menu '+' et puce "Image" retirable dans le composer ; rendu artéfact image pleine largeur avec ratio conservé sans animation, métadonnées (prompt, model, seed, aspectRatio), actions (ouvrir, télécharger, copie binaire, régénérer, réutiliser en pièce jointe) ; protection SSRF stricte (HTTPS uniquement, filtrage DNS IP privées/loopback/link-local, magic bytes PNG/JPEG/WEBP/GIF, téléchargement navigateur interdit) ; purge physique disque des fichiers d'artéfacts avec la conversation. | **§10, §26** | M1 à M5 | **Fait (100%)** | Bas |
| **M7** | **Génération et streaming de vidéos** : Cycle de vie asynchrone (`jobId`, statuts `queued`, `processing`, `completed`, `failed`, `cancelled`, persistance SQLite `video_jobs`, reprise `resumePendingJobs`), adaptateurs Google Veo, Replicate, fal.ai et Mock déterministe ; streaming vidéo sécurisé par requêtes HTTP Range (`206 Partial Content`), tickets d'accès éphémères (5 min, non loggués, sans token dans l'URL), validation Magic Bytes MP4 (`ftyp`), WEBM, MOV et protection SSRF stricte ; outil `generate_video` de niveau MEDIUM interactif ; intégration sobre in-chat (menu '+' "Créer une vidéo", puce "Vidéo" retirable [X], carte de suivi avec durée écoulée et arrêt, lecteur natif `<video controls preload="metadata">` sans autoplay). | **§10, §26** | M1 à M6 | **Fait (100%)** | Bas |
| **M8.0** | **Circuit fermé & Zéro dépendance externe** : Éradication Supabase, polices locales WOFF2 (64 fichiers, 0 appel fonts.googleapis.com), garde réseau daemon `docs/NETWORK.md`, en-têtes HTTP de sécurité durcis (`nosniff`, `frame-ancestors`, `no-referrer`, CSP). | **§26** | M1 à M7 | **Fait (100%)** | Bas |
| **M8.1** | **Nettoyage & Ergonomie sobre** : Éradication des composants et types orphelins (TasksWorkspace, ProvidersSettings, Persona, etc.), retrait de `pointer-events-none`, point d'accès unique dynamique aux Paramètres (sidebar / topbar conditionnel), dépliage historique avec repli à 10, redirection Personnaliser vers Compétences, onglets inspecteur conditionnels réels, catégories dynamiques Tool Registry dans composer. | **§3, §4, §6** | M8.0 | **Fait (100%)** | Bas |
| **M8.2** | **Production, Robustesse & Continuité** : Démarrage unique unifié `npm start` (`dist/index.html` + `dist-server/index.js`), confinement statique strict anti-traversée (`..`, `%2e%2e`) et blocage fichiers cachés, arrêt propre garanti sur `SIGINT`/`SIGTERM` avec `processManager.terminateAll()`, reconnexion continue indéfinie backoff max 10s, instructions personnalisées (max 4000 car, anti-secrets, balise `<custom_instructions>` subordonnée), capacités modèles complètes multi-modales, sauvegarde transactionnelle SQLite (`VACUUM INTO`, streaming `zip-stream`, exclusion clés par défaut, manifest) et restauration sécurisée (anti zip-slip, `.pre-restore.bak`), diagnostic système anonymisé sans secret, accessibilité universelle `:focus-visible` 2px décalé 2px couleur token et région `aria-live="polite"` pour streaming cadencé par phrase. | **§6, §10, §12, §26, §34** | M8.1 | **Fait (100%)** | Bas |
| **M8.3** | **Recherche FTS5, Actions Messages, Arrière-Plan, Contexte, Espace Disque & Tests E2E** : SQLite FTS5 (`search_fts`, sanitisation anti-opérateurs bruts, triggers automatiques conversations/messages/artéfacts), actions complètes sur les messages (copier, régénérer, suppression en cascade attachments/artéfacts avec confirmation, modification/troncature avec avertissement sur fichiers modifiés en mode Code), travail en arrière-plan (indicateur d'activité fixe 8px, plafond `max_concurrent_tasks` configurable), suivi du contexte (ligne 12px dès 60%, résumé automatique à 80% avec séparateur sobre "Contexte résumé"), espace disque (Paramètres › Confidentialité, volumes réels 5 catégories, compactage SQLite VACUUM et nettoyage sécurisé avec confirmation explicite), serveur mock OpenAI local avec streaming SSE et suite E2E Playwright. | **§6, §10, §21, §26, §34** | M8.2 | **Fait (100%)** | Bas |
| **M9** | **Design System, Tokenisation \u0026 Modularisation Paramètres** : Éradication 100% des couleurs en dur au profit des tokens CSS (`npm run lint:tokens` 0 violation), modularisation de `ClaudeSettingsModal` en 10 pages (`src/features/settings/pages/`) et 10 hooks (`src/hooks/settings/`) avec respect absolu de la règle des 400 lignes par fichier (tous les fichiers < 400 lignes), thème clair intégré et validé avec vérification mathématique des contrastes WCAG AA (> 4.5:1 / > 3:1) et 0 régression visuelle sur les 5 captures sombres existantes. | **§2, §3, §34** | M8.3 | **Fait (100%)** | Bas |
| **M10.0** | **Correctif blocage Composer après ajout de clé** : La cause racine était `useEffect([], [])` dans `ClaudeComposer.tsx` — le catalogue `/api/models` n'était chargé qu'une seule fois au montage. Fix : `modelsRefreshKey` dans `AppContext`, `refreshModels()` appelé par `useProvidersSettings` après `addKey`/`removeKey`, dépendance ajoutée dans le `useEffect`, 4 états de message distincts (chargement, erreur réseau, aucune clé, prêt), auto-sélection du premier modèle. 7 tests de régression ajoutés. | **§10** | M9 | **Fait (100%)** | Bas |
| **M10.1** | **Isolation étanche des tests et des données réelles** : Les tests polluaient `%APPDATA%/iroko/` faute de préchargement d'environnement. Correctif : `tests/setup_test_env.js` injectant `IROKO_DATA_DIR` temporaire nettoyé à la sortie, garde `RuntimeDatabase` levant une erreur si instanciée sur les données réelles en test, `ui_check.mjs` isolé via routes Playwright sans altérer SQLite, 7 tests de régression (`tests/real_data_isolation.test.mjs`). Nettoyage de la base réelle préparé et soumis à validation. | **§21, §26** | M10.0 | **Fait (100%)** | Bas |
| **M10.2** | **Architecture Fournisseurs & Catalogue Curé** : Implémentation des 12 fournisseurs (§10 & §26) avec validation dédiée (endpoint `/auth/key` pour OpenRouter), normalisation des éditeurs et capacités, déduplication de versions, calcul dynamique des tiers de prix (`free`, `budget`, `standard`, `premium`), quotas de curation (max 2/éditeur/palier), persistance SQLite v13 (`model_catalog`), endpoints `/api/providers`, `/api/models`, `/api/models/refresh`, `/api/models/preferences`, événements WS `providers_changed` et `catalog_updated`. 13 tests unitaires PASS, 0 régression. | **§10, §26** | M10.1 | **Fait (100%)** | Bas |
| **M10.3** | **Sélecteur & États du Composer** : Source unique d'état réactive (`ModelStore` synchronisé via événements WS `providers_changed` et `catalog_updated`), 5 états réactifs du composer (sans fournisseur avec lien vers Paramètres, chargement, erreur + retry, indisponible avec proposition, prêt), auto-sélection de modèle, menu déroulant du sélecteur accessible (sections Favoris/Récents(3)/Fournisseurs prêts, tags sobres sans montant, détection d'incompatibilité en direct, navigation clavier), page Fournisseurs & Clés (12 presets, statuts normalisés en texte gris) et volet "Gérer les modèles" (catalogue complet, recherche, favoris, masquage, prix exacts de l'API). 9 tests automatisés dédiés PASS, 288/288 tests globaux PASS, 0 régression UI. | **§10, §26** | M10.2 | **Fait (100%)** | Bas |
| **U0** | **Référentiel UX 2026 & Outillage d'Audit Automatisé** : Norme WCAG 2.2 AA (`docs/UX_STANDARDS.md`), outillage devDependencies (`@axe-core/playwright`, `rollup-plugin-visualizer`, `web-vitals`), faux fournisseur SSE local déterministe, générateur de fixtures isolées 50/200/1000 messages, scripts d'audit `audit:a11y`, `audit:perf`, `audit:responsive`, `audit:tasks`, `audit:all` et baseline initiale. | **Règles UX U0** | M10.3 | **Fait (100%)** | Bas |
| **U1** | **Audit d'Accessibilité (WCAG 2.2 AA & Chat)** : Audit lecture seule sans modification applicative ; axe-core multi-viewports/forced-colors, matrice des contrastes des tokens, tailles des cibles interactives, navigation clavier seul, inspection sémantique HTML5/ARIA, mutations live streaming 5000 tokens, zoom 200%/400%, protocole de test manuel 12 tâches NVDA/Narrateur (`docs/audit/a11y/PROTOCOLE_MANUEL.md`) et rapport complet qualifié par niveau de preuve MESURÉ / OBSERVÉ / SUPPOSÉ (`docs/audit/a11y/RAPPORT.md`). | **Règles UX U1** | U0 | **Fait (100%)** | Bas |
| **U2** | **Audit de Performance Ressentie, Fluidité et Animations** : Audit lecture seule sur le build de production (`npm start`, `http://127.0.0.1:3001`) ; INP des 12 interactions (10 runs, CPU 1x et 4x, p50/p75), analyse streaming Markdown + scroll smooth (reparse complet à chaque token, retokenisation CodeBlock, scroll JS en boucle), longues conversations 50/200/500 messages (absence de virtualisation, restauration scroll), chargement à froid (LCP/CLS/TBT, bundle 474,6 Ko > budget 250 Ko, polices système, lazy loading Paramètres), inventaire complet des animations CSS/JS (prefers-reduced-motion conforme), repos et runtime (CPU à vide 0,1%, latence API p50 ≤ 2ms, poll `/api/agent/active-tasks` toutes les 2,5s). 19 constats classifiés MESURÉ/OBSERVÉ/SUPPOSÉ dans `docs/audit/perf/RAPPORT.md`. | **Règles UX U2** | U1 | **Fait (100%)** | Bas |
| **U3** | **Audit de Responsivité, Parcours et Microcopie** : Audit lecture seule ; matrice complète de 9 viewports (320px à 2560px, paysage 844x390, 0 débordement horizontal constaté, cibles tactiles, tiroir mobile, modale sur mobile, safe-areas, visualViewport), mesure chronométrée de 10 tâches utilisateur clés (temps, actions, clics, frappes vs cibles UX), diagnostic des états et de 8 scénarios de résilience, analyse du focus, défilement et saisie IME, audit exhaustif de microcopie (263 chaînes extraites, 100% vouvoiement, ellipse typographique, espaces insécables avant ponctuation double, glossaire normé). 10 constats classifiés RESP-01 à RESP-10 dans `docs/audit/responsive/RAPPORT.md` et protocole physique dans `docs/audit/responsive/PROTOCOLE_MOBILE.md`. | **Règles UX U3** | U2 | **Fait (100%)** | Bas |
| **U4** | **Synthèse Générale de l'Audit UX 2026** : Assemblage et consolidation globale des audits U1, U2 et U3 ; tableau des notes sur 100 par axe (13 axes, note globale pondérée 72.5/100, ventilation MESURÉ / OBSERVÉ / SUPPOSÉ), 25 fiches de constats normalisées (A11Y-01..12, PERF-01..20, RESP-01..10), matrice de priorisation Impact × Facilité découpée en 6 lots chirurgicaux indépendants, analyse de 3 chantiers structurants, 5 opportunités de différenciation stratégique et spécification des protocoles de bancs d'essai manuels utilisateur. Rapport unique de référence dans `docs/audit/RAPPORT_UX_2026.md`. | **Règles UX U4** | U3 | **Fait (100%)** | Bas |
| **Lot 1** | **Correctif Contrastes Sombre & Indicateur de Focus (WCAG 1.4.3, 1.4.11, 2.4.7)** : Rehausse des contrastes sombres au-dessus de 4,5:1 (`--text-tertiary: #959390`, `--text-placeholder: #959390`, `--text-secondary: #bcbab5`), nouveau token de focus de champ `--border-focus-field: #767470` (≥ 3,0:1), indicateur de focus universel 2px décalé 2px couleur token, décalage négatif anti-rognage dans la sidebar (WCAG 2.4.11), suppression outline sur champs texte au profit de la bordure du conteneur `[data-field-container]`, support de `forced-colors: active` avec `Highlight`. 0 violation axe-core sur 19 configurations. | **WCAG 2.2 AA** | U4 | **Fait (100%)** | Bas |
| **Lot 2** | **Correctif Calques, Actions Accessibles & Titre de Page (WCAG 2.1.1, 2.1.2, 2.4.2, 2.4.3)** : Primitive universelle `useOverlayFocus` (pile de calques, boucle Tab/Shift+Tab, capture Échap, attributs `inert` natifs sans Radix, restitution focus au déclencheur), modale des paramètres accessible (`role="dialog"`, `aria-modal="true"`, 0 fuite clavier sur 20 Tab), tiroir mobile inerte et accessible, sidebar repliée 100% inerte (`inert`), actions de messages accessibles au clavier (`focus-within:opacity-100`) et sur terminaux tactiles sans hover (`@media(hover:none)`), titre dynamique de page `document.title` synchronisé avec le sujet de conversation actif. | **WCAG 2.2 AA** | Lot 1 | **Fait (100%)** | Bas |
| **Lot 3** | **Correctif Annonces, Repères, Noms et États Accessibles, Cibles (WCAG 1.3.1, 2.4.1, 2.5.8, 4.1.2, 4.1.3 ; Règles UX U3 & U6)** : Repères sémantiques HTML5 (`<header>` topbar, `<nav aria-label="Navigation">` sidebar, `<main id="main-content">` contenu principal, lien d'évitement "Aller au contenu" `sr-only focus:not-sr-only`), régions en direct centralisées (`role="status"` polie et `role="alert"` assertive dans `ZyriconAppShell`, hook `useLiveAnnouncements` cadencé strictement aux 4 transitions de phases sans mise à jour par token), contrôle segmenté Chat \| Code accessible (`role="group"`, `aria-label="Mode"`, `aria-pressed`), boutons enrichis de noms et descriptions (`aria-label`, `aria-description`, `aria-expanded`), structuration des messages en `<article>` avec titres sémantiques masqués `<h3>`, cibles interactives compactes étendues à 24×24 px (et 44×44 px sur tactile) sans altération visuelle via pseudo-élément `.tap-target-24`. | **WCAG 2.2 AA** | Lot 2 | **Fait (100%)** | Bas |
| **Lot 4** | **Correctif Rendu du Streaming et Défilement (WCAG 2.3.3, Règles UX U5 & U7)** : Tampon de flux `useStreamBuffer` (accumulation des deltas, rAF ≤ 20 fps / 50ms min, flush immédiat en fin de tâche), blocs mémoïsés `MemoizedBlock` et découpage de premier niveau `markdownParser` avec cache incrémental, coloration syntaxique monochrome de code unique à la fermeture (`isOpen` / `isClosed`, désactivée > 2000 lignes), défilement intelligent stick-to-bottom `useStickToBottom` (seuil 48px, écouteurs passifs, 0 scroll forcé si utilisateur remonté, défilement `auto` en streaming et respect strict de `animations === 'reduced'` et `prefers-reduced-motion`), bouton "Revenir en bas" accessible [À VALIDER] (0 ombre, tokens sobres). 5 tests dédiés PASS, 305/305 tests PASS, 0 diff ui:check. | **WCAG 2.3.3 / UX** | Lot 3 | **Fait (100%)** | Bas |
| **Lot 5** | **Correctif Ergonomie Mobile & Rétention des Brouillons (Règles UX U1, U3, U8, U9, constats F-12, F-13, F-14)** : Taille de police minimale de 16 px sous `@media (pointer: coarse)` sans `!important` sur textarea, inputs et modale (suppression du zoom automatique iOS sans désactiver le zoom utilisateur, zéro `maximum-scale`) ; gestion du clavier virtuel par `useVisualViewportHeight` ajustant `--app-height` (repli `100dvh`, rAF) et recentrage du champ actif au focus via `scrollIntoView` ; support complet des zones sûres `env(safe-area-inset-*)` (portrait, composer, paysage 844×390) ; cibles tactiles minimales 44×44 px sous `pointer: coarse` via pseudo-élément `.tap-target-24::before` sans altérer la taille visuelle ; persistance automatique des brouillons `useDraft` (localStorage, debounce 400 ms, restauration au montage/changement de fil, effacement immédiat à l'envoi, limitation à 50 brouillons et rétention 30 jours, effacement complet lors des purges de confidentialité). 4 tests dédiés PASS, 309/309 tests globaux PASS, `npm run ui:check` 7/7 PASS. | **WCAG 2.2 / UX Mobile** | Lot 4 | **Fait (100%)** | Bas |





---

## Protocole de Clôture de Lot

Pour chaque lot implémenté :
1. `npm test` au vert (tests de sécurité, chemins, permissions, masquage de secrets).
2. `npm run ui:check` au vert : **0,00 % de différence** sur les états existants.
3. `npm run lint` (`tsc --noEmit`) : 0 erreur.
4. `npm run build` : 0 erreur de compilation Vite.
5. Actualisation conjointe de `docs/FEATURES.md`, `docs/ROADMAP.md` et `docs/PLAN_EXECUTION.md`.
