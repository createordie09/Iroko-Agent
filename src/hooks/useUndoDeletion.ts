/**
 * Hook useUndoDeletion — Re-export pour l'accès au contexte de suppression différée
 */
export { useUndoDeletion } from '../context/UndoDeletionContext';
export type { ActiveDeletionState, ScheduleDeletionOptions } from '../context/UndoDeletionContext';
